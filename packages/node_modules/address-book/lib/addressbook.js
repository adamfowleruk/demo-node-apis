const express = require('express')
const app = express()
const port = 3000 // TODO get this from Environment PORT variable

module.exports = {
  start: function (settings) {
    app.get('/', (req, res) => {
      res.send({
        "message": "Ay up me duck",
        "address-count": 0
      })
    })

    app.listen(settings.apiPort, () => {
      console.log(`Address book listening at http://localhost:${settings.apiPort}`)
    })

  }
};